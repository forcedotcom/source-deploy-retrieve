import { convert } from '../../src/convert';

describe('Convert', () => {
  it('should write a manifest to the output', () => {});

  it('should generate package name using timestamp when option omitted', () => {});

  it('should ensure directory exists before outputting package', () => {});

  it('should create a pipeline with proper stream configuration', () => {});
});
